import { Box, styled } from '@mui/material';

export const StyledPageResultSection = styled(Box)(({ theme }) => ({
  padding: theme.spacing(8, 0, 14, 0),
  backgroundColor: theme.palette.misc.main,
}));
